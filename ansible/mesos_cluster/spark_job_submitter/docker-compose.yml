version: '2'
services:
  spark-job-submitter:
    build: .
    image: spark-job-submitter-image
    restart: always
    container_name: spark-job-submitter
    network_mode: host
    privileged: true



